import { Link } from "react-router-dom"
import Nav from "./Nav"
import Layout from "./Layout"
export default function Header(){
    return(
        <header>
                <Layout />
        </header>
    )
}